<!--index.wxml-->
<view class="content">
  <view class='apilist' wx:for="{{apiList}}" wx:key="{{index}}" bindtap='currentTap' data-item="{{item}}">
    <view class='apiItem'>{{item}}
    </view>
    
  </view>
  <button open-type="getUserInfo" bindtap='getUserInfoTap'>点击获取用户信息授权</button>
</view>